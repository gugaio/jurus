# reverse-proxy.conf

server {
    listen 8020;
    server_name jurus;

    location / {
    	proxy_pass http://jurus:8010;
#	resolver 127.0.0.1:8020 ipv6=off valid=10s;
        proxy_set_header Host $host;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }
}
